1. создать компонент UsersContainer и внутри вывести в консоль содержимое массива
users
2. создать state usersList и пройтись по нему map-ом 
3. для каждого пользователя создать параграф с его именем
4. создать компонет UserItem который возвращает следующую структуру
<div>
    <p>имя</p>
    <p>фамилия</p>
</div>

5. использовать UserItem при генерации карточек пользователей вместо параграфа